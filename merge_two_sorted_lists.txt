# Problem: Merge Two Sorted Lists
# Write a function mergeTwoLists(list1, list2) that takes two sorted lists of integers
# and returns a new sorted list containing all elements from both input lists.
# - Do not use sort() at the end.
# - Both input lists are already sorted in ascending order.
# - Do not modify the original input lists.
# - You can use list methods like insert(), append(), or extend().
# Example:
# list1 = [1, 2, 4]
# list2 = [1, 3, 4]
# Output: [1, 1, 2, 3, 4, 4]
